const AuctionUpdateMessage = ({ message }) => {
    return (
        <div className="auction-update-message">
            <p>{message}</p>
        </div>
    );
};

export default AuctionUpdateMessage;
